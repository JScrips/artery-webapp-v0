const Gallery = () => {
  // create a gallery page that will display all of the images that has been uploaded to the database. It should utilize a carousel to display the images.

  return (
    <div>
      <h1>Gallery</h1>
    </div>
  );
};

export default Gallery;
